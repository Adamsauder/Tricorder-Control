import React from 'react';

// Simple approach: just import the component directly
// If there are issues, they'll show up in the build or runtime
import PropTypeGroupDashboard from './components/PropTypeGroupDashboard';

function App() {
  return <PropTypeGroupDashboard />;
}

export default App;
